[build]
dockerfilePath = "planner/Dockerfile"
dockerContext = "planner"

[deploy]
healthcheckPath = "/api/sam-ranges"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
