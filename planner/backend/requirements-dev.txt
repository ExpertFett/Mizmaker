# Dev/test dependencies — not required for production.
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

# pytest pinned to <9 — pytest 9 (released 2025-11) changed the default
# import mode in a way that broke `from planner.backend.services.X` style
# imports across our test files. CI silently rode pytest 8 until
# 2026-05-04 when the GHA pip cache TTL'd out, then resolved to 9 and
# every push went red for ~24 runs (see commit 7f44a4d for the fix).
# Holding the upper bound until we migrate the test suite to 9.
pytest>=8.0,<9
# pytest-cov 6.0 requires pytest>=9, so it must stay <6 in lock-step
# with the pytest pin above. Bump both together when migrating.
pytest-cov>=5.0,<6
pyyaml>=6.0          # test scenarios
